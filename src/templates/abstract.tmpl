abstract $name = {

 cat 

   $proposition.name;
   $entity.name;

  #for $pre in $predefined
   $pre.name;
  #end for

  #for $c in $classes 
   $c.type; 
  #end for
  #for $c in $funclasses
   $c.type;
  #end for

 fun
   
  #for $c in $classes
   $c.name : $c.type; 
  #end for

  #for $c in $funclasses
   $c.name : $c.domain -> $c.type;
  #end for

  #for $p in $functions
#if type($p.range) == list
#set $range = ''
 #for $r in $p.range
 #set $range = $range + str($r) + ' -> '
 #end for
 #set $range = $range.strip()
#else
#set $range = $p.range + ' -> '
#end if

   $p.name : $p.domain -> $range $proposition.name;

  #end for
}
