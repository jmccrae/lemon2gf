--# -path=.:prelude:present

concrete Lexical$name$lang of $name = Core$lang ** open Syntax$lang, Paradigms$lang in {

lincat

  #for $pre in $signature.predefined
   $pre.name = <>;
  #end for

  #for $c in $signature.categories
   $c.type = <>;
  #end for

lin

#for $l in $linearizations
  $l.ref $l.args = #slurp
#if $l['isRecord'] 
{
#end if
#for $lin in $l.lin
#if $l['isRecord']
       $lin.field = #slurp
#end if
#if $len($lin.value) == 1
$lin.value[0] ; 
#else
variants { #slurp
#for $v in $lin.value
$v; #slurp
#end for
} ;
#end if
#end for
#if $l['isRecord']
   } ;
#end if

#end for

oper
  #for $o in $opers
   $o
  #end for

}
